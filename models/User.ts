export interface Interface {
    UUID:string;
    department:string;
    mobile:string;
    name: string;
}

export const Schema = {
    UUID: String,
    department: String,
    mobile: String,
    name: String,
};
