export interface IUser {
    UUID:number;
    department:string;
    mobile:string;
    name: string;
}

export const SUser = {
    UUID: String,
    department: String,
    mobile: String,
    name: String,
};